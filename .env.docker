# Environment variables for Docker deployment
# Copy this file to .env and customize as needed

# Port for the application (default: 3000)
PORT=3000

# Example deployment on different ports:
# PORT=8080    # For port 8080
# PORT=80      # For port 80 (requires root or port redirection)

# Build-time environment variables (set these before running ./docker-build.sh)
# Uncomment and configure if you want to enable the chat widget
# VITE_CHATWOOT_HOST_URL=https://your-chatwoot-instance.com
# VITE_CHATWOOT_WEBSITE_TOKEN=your-website-token

# Base path (usually / for Docker, /CozyCritters/ for GitHub Pages)
# VITE_BASE=/